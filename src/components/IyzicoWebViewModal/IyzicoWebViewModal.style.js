import { StyleSheet } from "react-native";

export default StyleSheet.create({
    webViewClose: {
        flexDirection: "row",
        alignItems: "center",
        gap: 8,
        padding: 16,
        borderBottomWidth: 1,
        borderBottomColor: "#eee",
    },
    webViewCloseText: {
        fontSize: 16,
        fontWeight: "600",
        color: "#333",
    },
});